import { Left, SectionRegister } from "../components";

const Register = () => {
  return (
    <div className="min-h-screen flex">
      <Left />
      <SectionRegister />
    </div>
  );
};

export default Register;
