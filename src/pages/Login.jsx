import { Left, SectionLogin } from "/src/components";

const Login = () => {
  return (
    <div className="min-h-screen flex">
      <Left />
      <SectionLogin />
    </div>
  );
};

export default Login;
